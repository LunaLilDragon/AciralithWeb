<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        if title
          title Aciralith - #{title}
        else
          title Aciralith
        script(src="/script/jquery-3.5.0.js")
        link(rel="stylesheet", href="/style/desktop.css")
        block style
    body
      nav
        include nav.pug
      #case
        block content
